{
  "name": "autofines-ge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.mjs",
    "live": "node scripts/live.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
